export type InputPair = {
  name: string;
  quantity: number | "";
  units: string;
};

export type NutritionInput = {
  calories: string;
  protein: string;
  fat: string;
  carbs: string;
  fiber: string;
  sodium: string;
};
